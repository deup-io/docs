# BlockType

块类型, 枚举类型.

```typescript
enum BlockType {
  PAGE = 'page',
  TEXT = 'text',
  IMAGE = 'image',
  VIDEO = 'video',
}
```

### page

页面类型, `BlockType.PAGE`

### text

文本类型, `BlockType.TEXT`

### image

图片类型, `BlockType.IMAGE`

### video

视频类型, `BlockType.VIDEO`
